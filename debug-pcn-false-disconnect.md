# Debug Session: pcn-false-disconnect
- **Status**: [OPEN]
- **Issue**: 在新加坡 PCN 路径规划器中，用户选择的两个视觉上连通的点被误判为“不连通”。
- **Debug Server**: http://127.0.0.1:7777/event
- **Log File**: .dbg/trae-debug-log-pcn-false-disconnect.ndjson

## Reproduction Steps
1. 通过本地服务器打开 `http://127.0.0.1:8123/projects/singapore-map/planner.html`。
2. 在地图上选择两个或多个视觉上位于同一条连续 PCN 路线上的点。
3. 观察是否弹出“不连通”提示，或是否只生成部分本地 PCN 路径。

## Hypotheses & Verification
| ID | Hypothesis | Likelihood | Effort | Evidence |
|----|------------|------------|--------|----------|
| A | 点击点被吸附到了错误线段，导致起终点实际落在不同连通块 | High | Low | 已加日志，待用户复测 |
| B | 线段投影后只尝试两端点组合，遗漏了交汇处或近接跳接，导致本应连通的线段无法入图 | High | Medium | 已加日志，待用户复测 |
| C | 100 米近接连接只补到了节点层，没有覆盖到线段投影后的实际接入方式 | Medium | Medium | 已确认 |
| D | 图构建时存在几何切分或端点合并误差，某些肉眼连续的 PCN 在数据里仍被分成不同分量 | Medium | Medium | 已确认 |
| E | “不连通”判定发生在路径重建或分段汇总阶段，而不是最短路本身 | Low | Low | 暂未发现支持证据 |

## Log Evidence
- 离线图分析（修复前逻辑）：
  - 基础精确连边后共有 `75` 个原始连通分量。
  - 存在 `22995` 对“同一条 PCN 线路、相距 100 米内、但位于不同原始连通分量”的节点对。
  - 示例：`Western Adventure Loop` 上存在仅 `3.89m / 20.51m / 58.44m` 的断口，但因现有规则跳过了同线路节点补边，导致这些断口无法被桥接。
- 策略对比：
  - 现逻辑（跳过同 route title 的近接补边）：最终 `37` 个连通分量，最大连通块 `26113` 节点。
  - 粗放修复（允许所有 100 米内补边）：最终 `5` 个连通分量，但新增近接边过多（`823671`）。
  - 最小修复（只给“原始图中不同连通分量”的近接节点补边）：同样收敛到 `5` 个连通分量，但近接边降到 `249794`，比粗放修复更克制。

## Verification Conclusion
- 已确认根因：近接补边规则错误地把“同一条线路中的小断口”排除在外，导致视觉上连续、但数据上存在微小间隙的 PCN 被误判为不连通。
- 已应用最小修复：近接补边不再按“是否同线路”过滤，而是按“是否已经处于同一个原始连通分量”过滤，只桥接真正断开的近邻节点。
- 当前仍保留最小调试日志，等待用户通过本地服务器复测后决定是否清理。
